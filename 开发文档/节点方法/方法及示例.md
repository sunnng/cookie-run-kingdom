方法名称:根据节点的范围匹配
语法:bounds(l,t,r,b)
参数说明:l 左范围,t 上范围,r右范围,b下范围
返回值:返回一个选择器对象,该对象支持级联选择
local sel = bounds(30,212,236,250)
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end


方法名称:匹配该范围内的节点
语法:boundsInside(l,t,r,b)
参数说明:l 左范围,t 上范围,r右范围,b下范围
返回值:返回一个选择器对象,该对象支持级联选择
local sel = boundsInside(30,212,236,250)
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:是否可以被勾选来匹配
语法:checkable(b)
返回值:返回一个选择器对象,该对象支持级联选择
local sel = checkable(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:className())
end

方法名称:根据是否被勾选来匹配
语法:checked(b)
返回值:返回一个选择器对象,该对象支持级联选择
local sel = checked(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:className())
end

方法名称:根据节点的className属性全匹配
语法:className(str)
参数说明:str 字符串类型，指定要查找的具体控件的类名
返回值:返回一个选择器对象,该对象支持级联选择
local sel = className("android.widget.TextView")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:根据className属性所包含的字符串模糊匹配
语法:classNameContains(str)
参数说明:str 字符串类型，指定要查找className包含该字符的节点
返回值:返回一个选择器对象,该对象支持级联选择
local sel = classNameContains("android.widget")
local node = sel:findOne(10000)
if node~= nil then
	print(node:className())
end

方法名称:根据className属性后缀匹配
语法:classNameEndsWith(str)
参数说明:str 字符串类型，指定要查找className后缀为该字符的节点
返回值:返回一个选择器对象,该对象支持级联选择
local sel = classNameEndsWith("TextView")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:根据className属性正则匹配
语法:classNameMatches(str)
参数说明:str 字符串类型，指定要匹配className的正则表达式
返回值:返回一个选择器对象,该对象支持级联选择
local sel = classNameMatches("^android.*TextView$")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:根据className属性前缀匹配
语法:classNameStartsWith(str)
参数说明:str 字符串类型，指定要查找className前缀为该字符的节点
返回值:返回一个选择器对象,该对象支持级联选择
local sel = classNameStartsWith("android.widget")
local node = sel:findOne(10000)
if node~= nil then
	print(node:className())
end

方法名称:根据是否可点击匹配
语法:clickable(b)
返回值:返回一个选择器对象,该对象支持级联选择
local sel = clickable(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:className())
end

方法名称:根据深度索引匹配
语法:depth(level)
返回值:返回一个选择器对象,该对象支持级联选择
local sel = depth(1)
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:根据节点的desc属性全匹配
语法:desc(str)
参数说明:str 字符串类型，指定要查找的具体控件的desc
返回值:返回一个选择器对象,该对象支持级联选择
local sel = desc("懒人高级版")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:根据desc包含的部分字符串匹配
语法:descContains(str)
参数说明:str 字符串类型，指定要查找的模糊的包含desc某一段字符串的内容
返回值:返回一个选择器对象,该对象支持级联选择
local sel = descContains("懒人")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:根据desc的后缀去匹配
语法:descEndsWith(str)
参数说明:str 字符串类型，指定要查找的desc的后缀
返回值:返回一个选择器对象,该对象支持级联选择
local sel = descEndsWith("高级版")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:正则匹配desc
语法:descMatches(reg)
参数:reg 是一个正则字符串表达式，具体正则请参考java
返回值:返回一个选择器对象,该对象支持级联选择
local sel = descMatches(".*高级版$")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:根据desc的前缀去匹配
语法:descStartsWith(str)
参数说明:str 字符串类型，指定要查找的desc的前缀
返回值:返回一个选择器对象,该对象支持级联选择
local sel = descStartsWith("懒人")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:匹配该范围内的节点
语法:drawingOrder(level)
返回值:返回一个选择器对象,该对象支持级联选择
local sel = drawingOrder(1)
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:根据是否可用匹配
语法:enabled(b)
返回值:返回一个选择器对象,该对象支持级联选择
local sel = enabled(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:className())
end

方法名称:是否可以被勾选来匹配
语法:focusable(b)
返回值:返回一个选择器对象,该对象支持级联选择
local sel = focusable(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:className())
end

方法名称:根据是否抢占了焦点来匹配
语法:focused(b)
返回值:返回一个选择器对象,该对象支持级联选择
local sel = focused(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:className())
end

方法名称:根据节点的id属性全匹配
语法:id(str)
参数说明:str 字符串类型，指定要查找的具体控件的id
返回值:返回一个选择器对象,该对象支持级联选择
local sel = id("com.nx.nxprojit:id/detail_tv_title")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:根据id包含的部分字符串匹配
语法:idContains(str)
参数说明:str 字符串类型，指定要查找的模糊的包含id某一段字符串的内容
返回值:返回一个选择器对象,该对象支持级联选择
local sel = idContains("detail_tv_title")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:根据id的后缀去匹配
语法:idEndsWith(str)
参数说明:str 字符串类型，指定要查找的id的后缀
返回值:返回一个选择器对象,该对象支持级联选择
local sel = idEndsWith("detail_tv_title")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:正则匹配id
语法:idMatches(reg)
参数:reg 是一个正则字符串表达式，具体正则请参考java
返回值:返回一个选择器对象,该对象支持级联选择
local sel = idMatches(".*detail_tv_title$")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:idStartsWith 根据id的前缀去匹配
语法:idStartsWith(str)
参数说明:str 字符串类型，指定要查找的id的前缀
返回值:返回一个选择器对象,该对象支持级联选择
local sel = idStartsWith("com.nx.nxprojit")
local node = sel:findOne(10000)
if node~= nil then
	print(node:id())
end

方法名称:根据在当前父节点里面的索引顺序查找
语法:index(level)
返回值:返回一个选择器对象,该对象支持级联选择
local sel = index(1)
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:根据是否可长按点击匹配
语法:longClickable(b)
返回值:返回一个选择器对象,该对象支持级联选择
local sel = longClickable(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:className())
end

方法名称:获取当前节点的范围信息
语法:node:bounds()
返回值:返回4个整数，依次是左范围，上范围，右范围，下范围
local sel = className("android.widget.TextView")
local node = sel:findOne(10000)
if node ~= nil then
	print(node:bounds())
end

方法名称:获取当前节点相对父节点中的范围信息
语法:node:boundsInParent()
返回值:返回4个整数，依次是左范围，上范围，右范围，下范围
local sel = className("android.widget.TextView")
local node = sel:findOne(10000)
if node ~= nil then
	print(node:boundsInParent())
end

方法名称:获取当前节点的子节点个数
语法:node:node:childCount()
local sel = className("android.widget.LinearLayout")
local node = sel:findOne(10000)
if node~= nil then
	print(node:childCount())
end

方法名称:获取当前节点下的所有子节点
语法:node:childs()
local sel = bounds(0,36,540,960)
local node = sel:findOne(10000)
if node~= nil then
	local arr = node:childs()
	local cnt = node:childCount()
	print(cnt)
	for i=1,#arr do
		print(arr[i]:className())
	end
end

方法名称:获取节点的className属性值
语法:node:className()
返回值:返回className
local sel = className("android.widget.TextView")
local node = sel:findOne(10000)
if node ~= nil then
	print(node:className())
end

方法名称:抢占焦点
语法:node:focus()
返回值:返回布尔类型，true表示成功反正失败
local sel = className("android.widget.EditText")
local node = sel:findOne(10000)
if node~= nil then
	print(node:clearFocus())
end

方法名称:点击当前节点
语法:node:click()
返回值:返回布尔类型，true表示成功反正失败
local sel = text("懒人高级版")
local node = sel:findOne(10000)
if node~= nil then
	print(node:click())
end

方法名称:折叠控件
语法:node:collapse()
返回值:返回布尔类型，true表示成功反正失败
local sel = className("android.widget.ListView")
local node = sel:findOne(10000)
if node~= nil then
	print(node:collapse())
end

方法名称:右键点击节点
语法:node:node:contextClick()
返回值:返回布尔类型，true表示成功反正失败
local sel = className("android.widget.ListView")
local node = sel:findOne(10000)
if node~= nil then
	print(node:contextClick())
end

方法名称:获取输入框的文本到剪贴板
语法:node:copy
返回值:返回布尔类型，true表示成功反正失败
local sel = className("android.widget.EditText")
local node = sel:findOne(10000)
if node~= nil then
	print(node:copy())
end

方法名称:剪切输入框选中的内容
语法:node:cut
返回值:返回布尔类型，true表示成功反正失败
local sel = className("android.widget.EditText")
local node = sel:findOne(10000)
if node~= nil then
	print(node:cut())
end

方法名称:获取当前节点深度索引
语法:node:depth()
返回值:返回当前节点的绘制顺序的索引
local sel = className("android.widget.TextView")
local node = sel:findOne(10000)
if node~= nil then
	print(node:depth())
end

方法名称:获取节点的desc属性值
语法:node:desc()
返回值:返回desc
local sel = className("android.widget.TextView")
local node = sel:findOne(10000)
if node ~= nil then
	print(node:desc())
end

方法名称:获取当前节点的绘制顺序的索引
语法:node:drawingOrder()
返回值:返回当前节点的绘制顺序的索引
local sel = className("android.widget.TextView")
local node = sel:findOne(10000)
if node~= nil then
	print(node:drawingOrder())
end

方法名称:折叠控件
语法:node:expand()
返回值:返回布尔类型，true表示成功反正失败
local sel = className("android.widget.ListView")
local node = sel:findOne(10000)
if node~= nil then
	print(node:expand())
end

方法名称:抢占焦点
语法:node:focus()
返回值:返回布尔类型，true表示成功反正失败
local sel = className("android.widget.EditText")
local node = sel:findOne(10000)
if node~= nil then
	print(node:focus())
end

方法名称:获取节点的id属性值
语法:node:id()
返回值:返回id
local sel = className("android.widget.TextView")
local node = sel:findOne(10000)
if node ~= nil then
	print(node:id())
end

方法名称:长按当前节点
语法:node:longClick()
返回值:返回布尔类型，true表示成功反正失败
local sel = text("懒人高级版")
local node = sel:findOne(10000)
if node~= nil then
	print(node:longClick())
end

方法名称:获取节点的packageName属性值
语法:node:packageName()
返回值:返回packageName
local sel = className("android.widget.TextView")
local node = sel:findOne(10000)
if node ~= nil then
	print(node:packageName())
end

方法名称:获取父节点
语法:node:parent()
local sel=index(1):text("192.168.1.3"):desc("")
local node = sel:findOne(10000)
if node~= nil then
	local p = node:parent()
	print(p:bounds())
end

方法名称:粘贴剪贴板里面的内容到节点的输入框
语法:node:copy
返回值:返回布尔类型，true表示成功反正失败
local sel = className("android.widget.EditText")
local node = sel:findOne(10000)
if node~= nil then
	print(node:paste())
end

方法名称:向后滚动
语法:node:scrollBackward
返回值:返回布尔类型，true表示成功反正失败
local sel = scrollable(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:scrollBackward())
end

方法名称:向下滚动
语法:node:scrollDown()
返回值:返回布尔类型，true表示成功反正失败
local sel = scrollable(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:scrollDown())
end

方法名称:向前滚动
语法:node:scrollForward
返回值:返回布尔类型，true表示成功反正失败
local sel = scrollable(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:scrollForward())
end

方法名称:向左滚动
语法:node:scrollLeft()
返回值:返回布尔类型，true表示成功反正失败
local sel = scrollable(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:scrollLeft())
end

方法名称:向右滚动
语法:node:scrollRight()
返回值:返回布尔类型，true表示成功反正失败
local sel = scrollable(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:scrollRight())
end

方法名称:滚动滚动条控件到指定位置
语法:node:scrollTo(row,col)
参数:row如果是垂直滚动条则代表滚动到第几行，如果是水平滚动条则用col滚动到第几列
返回值:返回布尔类型，true表示成功反正失败
local sel = scrollable(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:scrollTo(100,0))
end

方法名称:向上滚动
语法:node:scrollUp()
返回值:返回布尔类型，true表示成功反正失败
local sel = scrollable(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:scrollUp())
end

方法名称:选择选项、标记复选框、选择单选按钮、选择列表项等操作
语法:node:select
返回值:返回布尔类型，true表示成功反正失败
local sel = selected(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:select())
end

方法名称:设置节点所在控件的进度条的进度
语法:node:setProgress(1.0)
参数:pos 浮点类型
返回值:返回布尔类型，true表示成功反正失败
local sel = className("android.widget.ProgressBar")
local node = sel:findOne(10000)
if node~= nil then
	print(node:setProgress(1.0))
end

方法名称:选择选项、标记复选框、选择单选按钮、选择列表项等操作
语法:node:setSelection(start,end)
参数:start,end 选中从第start到end的字符串
返回值:返回布尔类型，true表示成功反正失败
local sel = className("android.widget.EditText")
local node = sel:findOne(10000)
if node~= nil then
	print(node:setSelection(1,3))
end

方法名称:获取当前节点的绘制顺序的索引
语法:node:setText(str)
参数:str要输入的字符串
返回值:返回布尔类型，true表示成功反正失败
local sel = className("android.widget.EditText")
local node = sel:findOne(10000)
if node~= nil then
	print(node:setText("hello"))
end

方法名称:获取节点的text属性值
语法:node:text()
返回值:返回text
local sel = className("android.widget.TextView")
local node = sel:findOne(10000)
if node ~= nil then
	print(node:text())
end

方法名称:获取节点的所有属性以json字符串的形式
语法:node:toJson()
返回值:返回json字符串
local sel = className("android.widget.TextView")
local node = sel:findOne(10000)
if node ~= nil then
	print(node:toJson())
end

方法名称:自动关闭无障碍服务，只有root或者激活模式有效
语法:nodeLib.closeAccessibility()
返回值:布尔类型
nodeLib.closeAccessibility()

方法名称:获取当前界面节点xml数据
语法:nodeLib.getNodeXml()
返回值:返回xml字符串
local xml = nodeLib.getNodeXml()
if xml~= nil then
	print(xml)
end

方法名称:锁定节点到内存，后面使用所有节点查找函数将都在内存中的节点去查找不会更新除非解锁
语法:nodeLib.lockNode()
注意:这个方法是配合新版节点函数使用注意和keepNode(旧版的节点函数)区别开了
返回值:布尔类型
nodeLib.lockNode()
local sel = className("android.widget.TextView")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

sel = className("android.widget.TextView")
node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end
nodeLib.unlockNode()

方法名称:自动打开无障碍服务，root或者激活模式下会自动开启，其它模式下需要手动开启
语法:nodeLib.openAccessibility()
返回值:布尔类型
nodeLib.openAccessibility()

方法名称:导出当前界面节点数据
语法:nodeLib.saveNode(path)
返回值:布尔类型
local r = nodeLib.saveNode("/sdcard/test.node")
print(r)

方法名称:导出当前界面节点数据
语法:nodeLib.saveNodeNew(path)
返回值:布尔类型
local r = nodeLib.saveNodeNew("/sdcard/test.node")
print(r)

方法名称:锁定节点到内存
语法:nodeLib.lockNode()
注意:这个方法是配合新版节点函数使用注意和releaseNode(旧版的节点函数)区别开了
返回值:布尔类型
nodeLib.lockNode()
local sel = className("android.widget.TextView")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

sel = className("android.widget.TextView")
node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end
nodeLib.unlockNode()

方法名称:根据packageName属性全字段匹配
语法:packageName(str)
参数说明:str 字符串类型，指定要匹配节点所属的包名
返回值:返回一个选择器对象,该对象支持级联选择
local sel = packageName("com.nx.nxprojit"):className("android.widget.TextView")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:匹配包含指定字符串的的节点
语法:packageNameContains(str)
参数说明:str 字符串类型，包含该字符串的包名的节点将被匹配到
返回值:返回一个选择器对象,该对象支持级联选择
local sel = packageNameContains("nxprojit"):className("android.widget.TextView")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:匹配包名后缀为指定字符串的的节点
语法:packageNameEndsWith(str)
参数说明:str 字符串类型，包名为该字符的后缀的节点将被匹配到
返回值:返回一个选择器对象,该对象支持级联选择
local sel = packageNameEndsWith("nx.nxprojit"):className("android.widget.TextView")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:包名正则匹配
语法:packageNameMatches(str)
参数说明:str 字符串类型，根据这个正则字符串匹配所有符合规则包名的节点
返回值:返回一个选择器对象,该对象支持级联选择
local sel = packageNameMatches("com.nx.*"):className("android.widget.TextView")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:匹配包名前缀为指定字符串的的节点
语法:packageNameStartsWith(str)
参数说明:str 字符串类型，包名为该字符的前缀的节点将被匹配到
返回值:返回一个选择器对象,该对象支持级联选择
local sel = packageNameStartsWith("com.nx"):className("android.widget.TextView")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:根据是否是密码框匹配
语法:enabled(b)
返回值:返回一个选择器对象,该对象支持级联选择
local sel = password(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:className())
end

方法名称:根据是否可以滚动来匹配
语法:scrollable(b)
返回值:返回一个选择器对象,该对象支持级联选择
local sel = scrollable(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:className())
end

方法名称:点击所有匹配到的节点
语法:sel:click()
说明:此方法会根据选择器里面的匹配属性去匹配直到匹配到节点然后去点击，如果匹配结果为空那么将会一直等待
返回值:返回布尔类型，true表示成功反之失败
local sel = className("android.widget.TextView"):text("雷电游戏中心")
sel:click()

方法名称:获取第一个节点
语法:findAll([timeout])
参数说明:timeout 匹配超时时间单位是毫秒，如果不填写将一直等待直到匹配到结果才会返回
返回值:获取匹配到的所有节点对象
local sel = className("android.widget.TextView")
local node = sel:findAll(10000)
if node~= nil then
for i=1,#node do
	print(node[i]:text())
end
end

方法名称:根据索引获取匹配到的节点
语法:findOnce([index])
参数说明:index 整数型，表示要获取所有符合条件结果中的第几个，当index不写表示0也就是第一个节点
返回值:返回匹配到的一个节点对象
local sel = className("android.widget.TextView")
local node = sel:findOnce(1) --匹配查找到的第二个节点
if node~= nil then
	print(node:text())
end

方法名称:获取第一个节点
语法:findOne([timeout])
参数说明:timeout 匹配超时时间单位是毫秒，如果不填写将一直等待直到匹配到结果才会返回
返回值:返回匹配到的一个节点对象
local sel = className("android.widget.TextView")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:长按点击所有匹配到的节点
语法:sel:sel:longClick()
说明:此方法会根据选择器里面的匹配属性去匹配直到匹配到节点然后去长按点击，如果匹配结果为空那么将会一直等待
返回值:返回布尔类型，true表示成功反之失败
local sel = className("android.widget.TextView"):text("雷电游戏中心")
sel:longClick()

方法名称:根据是否可见匹配
语法:visibleToUser(b)
返回值:返回一个选择器对象,该对象支持级联选择
local sel = selected(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:className())
end

方法名称:根据节点的text属性全匹配
语法:text(str)
参数说明:str 字符串类型，指定要查找的具体控件的text
返回值:返回一个选择器对象,该对象支持级联选择
local sel = text("懒人高级版")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:根据text包含的部分字符串匹配
语法:textContains(str)
参数说明:str 字符串类型，指定要查找的模糊的包含text某一段字符串的内容
返回值:返回一个选择器对象,该对象支持级联选择
local sel = textContains("懒人")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:根据text的后缀去匹配
语法:textEndsWith(str)
参数说明:str 字符串类型，指定要查找的text的后缀
返回值:返回一个选择器对象,该对象支持级联选择
local sel = textEndsWith("高级版")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:正则匹配text
语法:textMatches(reg)
参数:reg 是一个正则字符串表达式，具体正则请参考java
返回值:返回一个选择器对象,该对象支持级联选择
local sel = textMatches(".*高级版$")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:根据text的前缀去匹配
语法:textStartsWith(str)
参数说明:str 字符串类型，指定要查找的text的前缀
返回值:返回一个选择器对象,该对象支持级联选择
local sel = textStartsWith("懒人")
local node = sel:findOne(10000)
if node~= nil then
	print(node:text())
end

方法名称:根据是否可见匹配
语法:visibleToUser(b)
返回值:返回一个选择器对象,该对象支持级联选择
local sel = visibleToUser(true)
local node = sel:findOne(10000)
if node~= nil then
	print(node:className())
end