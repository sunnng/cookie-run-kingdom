# 触控方法

- `imeLib.deleteChar()`: 输入法删除一个字符
- `imeLib.finishInput()`: 输入法模拟完成输入
- `imeLib.keyEvent(action,keycode)`: 输入法模拟按键事件
- `imeLib.lock()`: 锁定使用懒人输入法
- `imeLib.setText(text)`: 输入法模拟输入文字
- `imeLib.unlock()`: 解锁懒人输入法
- `inputText(text)`: 模拟输入文字
- `keyDown(keycode)`: 按下一个按键不弹起
- `keyPress(keycode)`: 按一下按键并弹起
- `keyUp(keycode)`: 弹起一个按键
- `longTap(x,y)`: 长点击坐标为x,y的点
- `setOnTouchListener(onTouchEvent)`: 获取用户触摸屏幕坐标
- `swipe(x1,y1,x2,y2,time)`: 模拟从一点滑动到另外一点
- `tap(x,y)`: 点击坐标为x,y的点
- `touchDown(id,x,y)`: 按住不放
- `touchMove(id,x,y)`: 模拟滑动
- `touchMoveEx(id,x,y,time)`: 模拟滑动增强版
- `touchUp(id)`: 弹起手指

# 动态UI方法

## 基础控件
- `ui.addButton(layout,name,text,[w],[h])`: 创建按钮
- `ui.addCheckBox(layout,name,text,[sel],[w],[h])`: 创建复选框
- `ui.addEditText(layout,name,text,[w],[h])`: 创建输入框
- `ui.addImageView(layout,name,path,[w],[h])`: 创建图片视图
- `ui.addLine(layout,name,[w],[h])`: 创建线控件
- `ui.addTextView(layout,name,text,[w],[h])`: 创建文本视图
- `ui.addWebView(layout,name,url,[w],[h])`: 创建网页视图
- `ui.callJs(layout,name,js)`: 调用webview中的JS函数

## 布局管理
- `ui.newLayout(name,[w],[h])`: 创建新布局
- `ui.newRow(layout,rowid,[w],[h])`: 布局换行
- `ui.show(name)`: 显示布局
- `ui.dismiss(name)`: 关闭布局

## 表格控件
- `ui.addTableView(layout,name,headers,data,[w],[h])`: 创建表格
- `ui.addTableViewRow(name,rowData)`: 添加表格行
- `ui.getTableViewRowCnt(name)`: 获取表格行数
- `ui.getTableViewSelectIndex(name)`: 获取表格选中行索引
- `ui.removeTableViewRow(name,row)`: 删除表格行

## 事件处理
- `ui.setOnClick(name,event)`: 设置点击事件
- `ui.setOnChange(name,event)`: 设置变化事件
- `ui.setOnBackEvent(name,event)`: 设置返回事件

## 高级功能
- `ui.beginUiQueue()`/`ui.endUiQueue()`: UI命令队列
- `ui.saveProfile(path)`/`ui.loadProfile(path)`: 保存/加载UI配置
- `ui.setImageViewEx(handle,image)`: 重设图像控件(使用Bitmap)

## 控件操作
- `ui.setEditHintText(name,hinttext)`: 设置输入框默认提示字符串
- `ui.setGravity(name,alignMode)`: 设置控件对齐方式
- `ui.setFullScreen(name)`: 设置控件宽度全屏

## 属性获取
- `ui.getText(name)`: 获取控件文字
- `ui.getTextColor(name)`: 获取文字颜色
- `ui.getValue(name)`: 获取控件值
- `ui.getVisible(name)`: 获取显示状态

## 控件重设
- `ui.setBackground(name,color)`: 设置控件背景颜色
- `ui.setButton(name,text,[w],[h])`: 重设按钮控件
- `ui.setCheckBox(name,text,sel,[w],[h])`: 重设多选框控件
- `ui.setEditText(name,text,[w],[h])`: 重设输入框控件
- `ui.setEnable(name,enable)`: 设置控件可用状态
- `ui.setImageView(name,path,[w],[h])`: 重设图像控件
- `ui.setImageViewEx(name,bmp,[w],[h])`: 重设图像控件(使用Bitmap)

# 节点方法

## 节点选择器
- `bounds(l,t,r,b)`: 根据范围匹配节点
- `boundsInside(l,t,r,b)`: 匹配范围内的节点
- `className(str)`: 根据类名匹配
- `classNameContains(str)`: 根据className包含字符串匹配
- `classNameEndsWith(str)`: 根据className后缀匹配
- `classNameMatches(str)`: 根据className正则匹配
- `classNameStartsWith(str)`: 根据className前缀匹配
- `id(str)`: 根据ID匹配
- `text(str)`: 根据文本匹配
- `desc(str)`: 根据描述匹配
- `scrollable(b)`: 匹配可滚动节点
- `checkable(b)`: 根据是否可勾选匹配
- `checked(b)`: 根据是否已勾选匹配
- `clickable(b)`: 根据是否可点击匹配
- `depth(level)`: 根据深度索引匹配
- `descContains(str)`: 根据描述包含字符串匹配
- `descEndsWith(str)`: 根据描述后缀匹配
- `descMatches(reg)`: 正则匹配描述
- `descStartsWith(str)`: 根据描述前缀匹配
- `drawingOrder(level)`: 根据绘制顺序匹配
- `enabled(b)`: 根据是否可用匹配
- `focusable(b)`: 根据是否可获得焦点匹配
- `focused(b)`: 根据是否已获得焦点匹配
- `idContains(str)`: 根据ID包含字符串匹配
- `idEndsWith(str)`: 根据ID后缀匹配
- `idMatches(reg)`: 正则匹配ID
- `idStartsWith(str)`: 根据ID前缀匹配
- `index(level)`: 根据索引顺序匹配
- `longClickable(b)`: 根据是否可长按匹配
- `packageName(str)`: 根据包名全匹配
- `packageNameContains(str)`: 根据包名包含字符串匹配
- `packageNameEndsWith(str)`: 根据包名后缀匹配
- `packageNameMatches(str)`: 正则匹配包名
- `packageNameStartsWith(str)`: 根据包名前缀匹配
- `password(b)`: 根据是否是密码框匹配
- `visibleToUser(b)`: 根据是否可见匹配

## 节点操作
- `node:click()`: 点击节点
- `node:longClick()`: 长按节点
- `node:setText(str)`: 设置文本
- `node:scrollTo(row,col)`: 滚动到位置
- `node:bounds()`: 获取节点范围
- `node:collapse()`: 折叠节点
- `node:contextClick()`: 右键点击节点
- `node:copy()`: 复制节点文本到剪贴板
- `node:cut()`: 剪切节点选中文本
- `node:expand()`: 展开节点
- `node:focus()`: 使节点获得焦点
- `node:paste()`: 粘贴剪贴板内容到节点
- `node:scrollBackward()`: 向后滚动
- `node:scrollDown()`: 向下滚动
- `node:scrollForward()`: 向前滚动
- `node:scrollLeft()`: 向左滚动
- `node:scrollRight()`: 向右滚动
- `node:scrollUp()`: 向上滚动
- `node:select()`: 选择节点选项
- `node:setProgress(pos)`: 设置进度条进度
- `node:setSelection(start,end)`: 设置文本选中范围
- `sel:click()`: 点击所有匹配节点
- `sel:longClick()`: 长按所有匹配节点

## 节点查找
- `sel:findOne(timeout)`: 查找单个节点
- `sel:findAll(timeout)`: 查找所有匹配节点
- `sel:findOnce(index)`: 按索引查找节点
- `nodeLib.findByIndex(indexs)`: 根据节点所在树的每个分支顺序查找
- `nodeLib.findNextNode(selector,fuzzy_match)`: 获取指定节点的下一个兄弟节点
- `nodeLib.findPreNode(selector,fuzzy_match)`: 获取指定节点的上一个兄弟节点

## 节点属性
- `node:text()`: 获取节点文本
- `node:className()`: 获取类名
- `node:id()`: 获取节点ID
- `node:childs()`: 获取子节点
- `node:toJson()`: 获取节点JSON数据
- `node:boundsInParent()`: 获取节点在父节点中的范围
- `node:childCount()`: 获取子节点数量
- `node:depth()`: 获取节点深度索引
- `node:drawingOrder()`: 获取节点绘制顺序
- `node:packageName()`: 获取节点包名
- `node:parent()`: 获取父节点

## 节点工具
- `nodeLib.getNodeXml()`: 获取界面XML
- `nodeLib.saveNode(path)`: 保存节点数据
- `nodeLib.saveNodeNew(path)`: 导出节点数据(新版)
- `nodeLib.lockNode()`: 锁定当前节点树
- `nodeLib.closeAccessibility()`: 关闭无障碍服务
- `nodeLib.openAccessibility()`: 打开无障碍服务
- `nodeLib.isSnapServiceOk()`: 检测截图服务是否开启
- `nodeLib.keepNode()`: 锁定当前界面到内存
- `nodeLib.releaseNode()`: 释放锁定在内存中的节点信息
- `nodeLib.setNodeMode(mode,enableGetInVisibleNode)`: 设置获取节点模式(完整参数版)

## 无障碍服务控制
- `nodeLib.isAccServiceOk()`: 检测无障碍服务是否开启
- `nodeLib.openAccessibility()`: 打开无障碍服务
- `nodeLib.closeAccessibility()`: 关闭无障碍服务

## 无障碍服务事件
- `nodeLib.setAccessibilityEventCallback(callback)`: 设置无障碍服务事件回调

# 设备方法

## 设备信息
- `getBrand()`: 获取设备品牌
- `getModel()`: 获取设备型号
- `getDisplaySize()`: 获取屏幕分辨率
- `getBatteryLevel()`: 获取电池电量
- `getDeviceId()`: 获取设备ID
- `getBoard()`: 获取主板号
- `getBootLoader()`: 获取BOOTLOADER版本号
- `getCpuAbi()`: 获取CPU支持类型
- `getCpuAbi2()`: 获取CPU支持类型(备用)
- `getCpuArch()`: 获取CPU架构(0=x86,1=arm,2=arm64)
- `getCurrentActivity()`: 获取最顶层activity名称
- `getDevice()`: 获取设备别名
- `getFingerprint()`: 获取编译版本号
- `getHardware()`: 获取硬件序列号
- `getId()`: 获取ID修订号
- `getInsallAppInfos()`: 获取所有安装app的详细信息
- `getInstalledApk()`: 获取已安装apk列表
- `getInstalledApps()`: 获取已安装app信息
- `getLrApi()`: 获取懒人开放的api接口
- `getManufacturer()`: 获取制造商代号
- `getOaid()`: 获取设备oaid
- `getOsVersionName()`: 获取系统版本名
- `getPackageName()`: 获取当前包名
- `getProduct()`: 获取产品代号
- `getRunEnvType()`: 获取运行环境类型(0=root/激活,1=无障碍)
- `getScriptVersion()`: 获取脚本版本号
- `getSdkVersion()`: 获取SDK版本
- `getSensorsInfo()`: 获取所有传感器信息
- `getSimSerialNumber()`: 获取SIM卡序列号
- `getSubscriberId()`: 获取设备imsi
- `getUIConfig()`: 读取UI配置
- `getWifiMac()`: 获取wifi mac地址
- `getWorkPath()`: 获取脚本工作目录

## 应用控制
- `runApp(package)`: 启动应用
- `stopApp(package)`: 停止应用
- `appIsRunning(package)`: 检查应用运行状态
- `frontAppName()`: 获取前台应用
- `appIsFront(package)`: 检查应用是否前台运行
- `checkIsDebug()`: 检查是否调试状态
- `installApk(path)`: 安装APK文件
- `installLrPkg(path)`: 安装懒人更新包

## 系统控制
- `setWifiEnable(state)`: 开关WiFi
- `setBTEnable(state)`: 开关蓝牙
- `vibrate(duration)`: 设备震动
- `lockScreen()`/`unLockScreen()`: 屏幕长亮控制
- `setAirplaneMode(state)`: 开关飞行模式
- `setDisplayPowerOff(state)`: 设置息屏运行

## 脚本控制
- `exitScript()`: 退出脚本
- `restartScript()`: 重启脚本
- `setStopCallBack(func)`: 设置停止回调
- `setAccessibilityEnvMode()`: 设置无障碍引擎模式
- `setHandleEnvMode()`: 设置手动引擎模式
- `setLogOff(state)`: 关闭日志输出
- `setRootEnvMode(state)`: 设置ROOT(激活)模式
- `setSnapCacheTime(ms)`: 设置截图缓存时间
- `setTimer(func,time,...)`: 定时执行方法
- `setUserEventCallBack(cb)`: 设置悬浮按钮菜单回调

## 文件操作
- `getSdPath()`: 获取SD卡路径
- `extractAssets()`: 释放资源文件
- `unZip()`: 解压文件
- `extractApkAssets(res,outdir)`: 释放APK资源文件

## 高级功能
- `exec(cmd)`: 执行系统命令
- `setDpiToVir(dpi)`: 设置虚拟DPI
- `setSnapCacheBitmap(bmp)`: 设置截图缓存位图
- `xpcall(func,errHandler)`: 保护模式调用函数
- `setDpiToRealy()`: 恢复原始DPI

# 交互方法

## 窗口管理
- `showUI(uifile,[w],[h],[onUIEvent])`: 显示自定义界面
- `showUIEx(uifile,[x],[y],[w],[h],[onUIEvent])`: 显示可定位窗口
- `closeWindow(handle,saveConfig)`: 关闭窗口
- `setUICheck(handle,checked)`: 设置多选框选中状态
- `setUIEnable(handle,enable)`: 设置控件可用状态
- `setUISelect(handle,index)`: 设置单选框/下拉框选中项
- `setUITextColor(handle,color)`: 设置控件文本颜色
- `setUIWebViewUrl(handle,url)`: 设置浏览器控件地址

## HUD显示
- `createHUD()`: 创建HUD实例
- `showHUD(id,text,size,color,bg,pos,x,y,width,height)`: 显示/更新HUD
- `hideHUD(id)`: 隐藏HUD

## 拖拽视图
- `createDragView(x,y,width,height,imagePath)`: 创建可拖动视图
- `getDragViewPos(handle)`: 获取视图位置
- `setDragViewOnClick(handle,callback)`: 设置点击回调
- `removeDragView(handle)`: 移除视图

## 消息提示
- `toast(text,[x,y,textsize])`: 显示消息
- `hideToast()`: 隐藏消息

## 控件操作
- `getUIText(handle,pageidx,idname)`: 获取控件文本
- `setUIText(handle,pageidx,idname,text)`: 设置控件文本
- `setUIBackground(handle,pageidx,idname,bg)`: 设置控件背景
- `setUIVisible(handle,pageidx,idname,visible)`: 设置控件可见性
- `setUISelect(handle,pageidx,idname,index)`: 设置单选框/下拉框选中项

# 控制台方法

- `console.show([isShow])`: 显示/隐藏控制台窗口
- `console.dismiss()`: 关闭控制台
- `console.setPos(x,y,[w],[h])`: 设置控制台位置和大小
- `console.setTitle(title)`: 设置控制台标题
- `console.println(level,log)`: 打印日志(1-6表示不同日志级别)
- `console.clearLog()`: 清除控制台日志
- `console.lockConsole()`/`unlockConsole()`: 锁定/解锁控制台窗口
- `console.showTitle([isShow])`: 显示/隐藏标题栏

# 加解密方法

## AES加密
- `cryptLib.aes_crypt(data, key, operation, mode, [iv], [padding])`: AES加密/解密
- `cryptLib.aes_ivgen()`: 生成随机初始化向量
- `cryptLib.aes_keygen(key_length)`: 生成随机AES密钥

## RSA加密
- `cryptLib.rsa_encrypt(data, key, is_public_key)`: RSA加密/签名
- `cryptLib.rsa_decrypt(data, key, is_public_key)`: RSA解密/验证
- `cryptLib.rsa_generate_key([key_bits])`: 生成RSA密钥对

# Go调用方法

- `gobridge.call(libpath, funcname, ...)`: 调用Go函数
  - `libpath`: Go编译的动态库路径
  - `funcname`: Go导出的函数名
  - `...`: 可变参数

- `gobridge.register(funcname, function)`: 注册Lua回调函数
  - `funcname`: 注册的函数名称
  - `function`: Lua回调函数

- `gobridge.tobytes(data)`: 转换数据为二进制格式(用于Go函数参数传递)

# 扩展方法

## 基础功能
- `MD5(str)`: 计算字符串MD5
- `fileMD5(path)`: 计算文件MD5
- `encodeBase64(str)`/`decodeBase64(str)`: Base64编解码
- `encodeUrl(str)`/`decodeUrl(str)`: URL编解码

## 文件操作
- `readFile(path)`: 读取文件
- `writeFile(path,str,[append])`: 写入文件
- `fileExist(path)`: 检查文件存在
- `fileSize(path)`: 获取文件大小
- `delfile(path)`: 删除文件
- `mkdir(path)`: 创建目录

## 图像处理
- `getImage(path)`: 读取图像数据
- `rotateImage(path,degrees)`: 旋转图像
- `scaleImage(src,dst,w,h)`: 缩放图像
- `binaryImage(src,dst)`: 图像二值化
- `qrDecode(path)`: 解析二维码
- `findMultiColor()`: 区域多点找色

## 数据库
- `mysql.connectSQL()`: 连接MySQL
- `mysql.executeSQL()`: 执行SQL
- `mysql.executeQuerySQL()`: 执行查询
- `mysql.closeSQL()`: 关闭连接

## 多线程
- `beginThread(func)`: 启动线程
- `thread:stopThread()`: 停止线程

## Java扩展
- `import(className)`: 导入Java类
- `LuaEngine.getContext()`: 获取Android上下文
- `LuaEngine.loadApk()`: 加载APK插件

## 扩展方法
- `binaryRect(save,l,t,r,b,[ThresholdTypes],[thresh],[maxthresh])`: 区域截图二值化
- `getFileBase64(path)`: 文件Base64编码
- `rotateImage(path,rotate)`: 图像旋转
- `scaleImage(src,dst,w,h)`: 图像缩放

# 数组方法

## 基本操作
- `#array`: 获取数组长度
- `table.concat(table [, sep])`: 数组元素连接为字符串
- `table.insert(table, [pos,] value)`: 插入元素
- `table.remove(table [, pos])`: 移除元素
- `pairs(table)`: 表迭代器

## 高级操作
- `table.sort(table [, comp])`: 数组排序
- `ipairs(table)`: 数组迭代器

# 数学方法

## 基础运算
- `math.abs(x)`: 绝对值
- `math.floor(x)`: 向下取整
- `math.ceil(x)`: 向上取整
- `math.sqrt(x)`: 平方根
- `math.fmod(a,b)`: 取余数
- `math.max(x, ...)`: 返回最大值
- `math.min(x, ...)`: 返回最小值
- `math.acos(x)`: 反余弦
- `math.asin(x)`: 反正弦
- `math.atan(x)`: 反正切

## 三角函数
- `math.sin(x)`: 正弦(弧度)
- `math.cos(x)`: 余弦(弧度)
- `math.tan(x)`: 正切(弧度)
- `math.rad(x)`: 角度转弧度
- `math.deg(x)`: 弧度转角度

## 高级运算
- `math.log(x)`: 自然对数
- `math.exp(x)`: e的x次方
- `math.modf(x)`: 分离整数和小数部分
- `math.random([m[,n]])`: 随机数
- `math.randomseed(x)`: 设置随机种子

## 类型转换
- `math.tointeger(x)`: 转为整数
- `math.type(x)`: 获取数字类型
- `math.ult(m,n)`: 无符号整数比较

# 时间方法

## 时间获取
- `getNetWorkTime()`: 获取网络时间(年-月-日_时-分-秒)
- `os.time([table])`: 获取时间戳(秒)
- `systemTime()`: 获取系统时间戳(毫秒)
- `tickCount()`: 获取脚本运行时间(毫秒)
- `os.date("*t", time)`: 获取时间戳的详细信息(返回包含年、月、日等字段的表)

## 时间格式化
- `os.date(format[, time])`: 格式化时间
  - `%Y`: 年(1998)
  - `%m`: 月(01-12)
  - `%d`: 日(01-31)
  - `%H`: 时(00-23)
  - `%M`: 分(00-59)
  - `%S`: 秒(00-60)
  - `%w`: 星期(0-6)

# 图色方法

## 基础图色操作
- `cmpColor(x,y,color,sim)`: 单点颜色比对
- `cmpColorEx(mul_color,sim)`: 多点颜色比对
- `findColor(x1,y1,x2,y2,color,dir,sim)`: 区域找色
- `findImage(x1,y1,x2,y2,pic_name,sim)`: 模板匹配找图

## 高级图像处理
- `LuaEngine.snapShot(l,t,r,b)`: Java层截图
- `findCircle(x1,y1,x2,y2,...)`: 霍夫变换找圆
- `binaryImage(src,dst)`: 图像二值化

## OCR相关
- `PaddleOcr.detect()`: PaddleOCR识别
- `createOcr(lang)`: 创建Tesseract OCR对象
- `ocrText(handle,x1,y1,x2,y2)`: OCR识别文字

## 颜色转换
- `colorToRGB(color)`: 颜色转RGB分量
- `colorDiff(color1,color2)`: 颜色差异计算

## 高级OCR功能
- `PaddleOcr.detectWithPadding()`: 带空白增强的OCR识别
- `PaddleOcr.loadNnccModel()`: 加载自定义NCNN模型
- `PaddleOcr.loadOnnxModel()`: 加载自定义ONNX模型

## 对象检测
- `YoloV5.detect()`: YOLOv5目标检测

## 增强函数
- `cmpColorExT()`: 数组参数多点比色
- `findColorT()`: 数组参数区域找色
- `findMultiColor()`: 区域多点找色

## 增强函数
- `cmpColorExT()`: 数组参数多点比色
- `findColorT()`: 数组参数区域找色

# 网络方法

## HTTP请求
- `httpGet(url,[timeout],[header])`: GET请求
- `httpPost(url,postdata,[timeout],[header])`: POST请求
- `asynHttpGet(callback,url,[timeout],[header])`: 异步GET
- `asynHttpPost(callback,url,postdata,[timeout],[header])`: 异步POST

## WebSocket
- `startWebSocket(url,onOpened,onClosed,onError,onRecv)`: 创建连接
- `sendWebSocket(handle,text)`: 发送消息
- `closeWebSocket(handle)`: 关闭连接

## 文件传输
- `downloadFile(url,savepath,[progress])`: 下载文件
- `uploadFile(url,uploadfile,[timeout])`: 上传文件

## 邮件功能
- `LuaEngine.sendMail()`: 发送普通邮件
- `LuaEngine.sendMailWithFile()`: 发送带附件邮件
- `LuaEngine.sendMailWithMultiFile()`: 发送多附件邮件

# 文件方法

## 文件操作
- `io.open(path,mode)`: 打开文件
- `io.input([file])`: 设置默认输入文件
- `io.output([file])`: 设置默认输出文件
- `io.close([handle])`: 关闭文件

## 读写操作
- `io.read([format])`: 读取文件内容
- `io.write(...)`: 写入文件内容
- `io.lines([filename])`: 文件行迭代器

## 文件控制
- `file:seek([whence][,offset])`: 移动文件指针
- `file:flush()`: 刷新写入缓冲区
- `file:setvbuf(mode[,size])`: 设置缓冲模式

# 无障碍相关方法

## 手势操作
- `Gesture:new()`: 创建手势对象
- `Gesture:addPath(path)`: 添加滑动路径
- `Gesture:dispatch([callback])`: 执行手势
- `Path:new()`: 创建路径对象
- `Path:addPoint(x,y)`: 添加路径点
- `Path:setDurTime(ms)`: 设置滑动时长
- `Path:setStartTime(ms)`: 设置起始时间

## 节点操作
- `nodeLib.click(node)`: 点击节点
- `nodeLib.setText(node,text)`: 设置节点文本
- `nodeLib.updateNode()`: 刷新节点

## 节点查找
- `nodeLib.findAll(selector)`: 查找所有匹配节点
- `nodeLib.findOne(selector)`: 查找单个节点
- `nodeLib.findChildNodes(node)`: 查找子节点
- `nodeLib.findParentNode(node)`: 查找父节点

## 节点查找增强
- `nodeLib.findByIndex(indexs)`: 根据节点所在树的每个分支顺序查找
- `nodeLib.findNextNode(selector,fuzzy_match)`: 获取指定节点的下一个兄弟节点
- `nodeLib.findPreNode(selector,fuzzy_match)`: 获取指定节点的上一个兄弟节点

## 节点属性
- `node:text()`: 获取节点文本
- `node:className()`: 获取类名
- `node:id()`: 获取节点ID
- `node:childs()`: 获取子节点
- `node:toJson()`: 获取节点JSON数据
- `node:boundsInParent()`: 获取节点在父节点中的范围
- `node:childCount()`: 获取子节点数量
- `node:depth()`: 获取节点深度索引
- `node:drawingOrder()`: 获取节点绘制顺序
- `node:packageName()`: 获取节点包名
- `node:parent()`: 获取父节点

## 节点工具
- `nodeLib.getNodeXml()`: 获取界面XML
- `nodeLib.saveNode(path)`: 保存节点数据
- `nodeLib.saveNodeNew(path)`: 导出节点数据(新版)
- `nodeLib.lockNode()`: 锁定当前节点树
- `nodeLib.closeAccessibility()`: 关闭无障碍服务
- `nodeLib.openAccessibility()`: 打开无障碍服务
- `nodeLib.isSnapServiceOk()`: 检测截图服务是否开启
- `nodeLib.keepNode()`: 锁定当前界面到内存
- `nodeLib.releaseNode()`: 释放锁定在内存中的节点信息
- `nodeLib.setNodeMode(mode,enableGetInVisibleNode)`: 设置获取节点模式(完整参数版)

## 服务控制增强
- `nodeLib.isSnapServiceOk()`: 检测截图服务是否开启
- `nodeLib.keepNode()`: 锁定当前界面到内存
- `nodeLib.releaseNode()`: 释放锁定在内存中的节点信息
- `nodeLib.setNodeMode(mode,enableGetInVisibleNode)`: 设置获取节点模式(完整参数版)

## 无障碍服务控制
- `nodeLib.isAccServiceOk()`: 检测无障碍服务是否开启
- `nodeLib.openAccessibility()`: 打开无障碍服务
- `nodeLib.closeAccessibility()`: 关闭无障碍服务

## 无障碍服务事件
- `nodeLib.setAccessibilityEventCallback(callback)`: 设置无障碍服务事件回调

# 虚拟屏幕方法

## 虚拟屏控制
- `virtualDisplay.createVirtualDisplay(w,h,dpi)`: 创建虚拟屏
- `virtualDisplay.closeVirtualDisplay(displayId)`: 关闭虚拟屏
- `virtualDisplay.switchToVirtualDisplay(displayId)`: 切换虚拟屏
- `virtualDisplay.showVirtualDisplay(displayId,config)`: 显示虚拟屏窗口

## 虚拟屏操作
- `virtualDisplay.runAppWithVirtualDisplay(pkg,displayId)`: 在虚拟屏运行应用
- `virtualDisplay.snapToBitmap(displayId,x,y,w,h)`: 虚拟屏截图

## 虚拟屏输入
- `virtualDisplay.tap(displayId,x,y)`: 虚拟屏点击
- `virtualDisplay.swipe(displayId,x1,y1,x2,y2,time)`: 虚拟屏滑动
- `virtualDisplay.keyPress(displayId,keycode)`: 虚拟屏按键

## 高级触控
- `virtualDisplay.touchDown(displayId,id,x,y)`: 虚拟屏按下
- `virtualDisplay.touchMove(displayId,id,x,y)`: 虚拟屏移动
- `virtualDisplay.touchUp(displayId,id)`: 虚拟屏抬起
- `virtualDisplay.touchMoveEx(displayId,id,x,y,time)`: 增强版虚拟屏滑动

# IMGUI方法

## 窗口管理
- `imgui.createWindow()`: 创建窗口
- `imgui.close()`: 关闭窗口
- `imgui.show()`: 显示窗口

## 控件创建
- `imgui.createButton()`: 创建按钮
- `imgui.createInputText()`: 创建输入框
- `imgui.createCheckBox()`: 创建复选框
- `imgui.createSlider()`: 创建滑动条
- `imgui.createProgressBar()`: 创建进度条
- `imgui.createLine()`: 创建线控件

## 布局管理
- `imgui.createVerticalLayout()`: 创建垂直布局
- `imgui.createHorticalLayout()`: 创建水平布局

## 图形绘制
- `imgui.createLine()`: 绘制线段
- `imgui.createRectangle()`: 绘制矩形
- `imgui.createCircle()`: 绘制圆形

## 事件处理
- `imgui.setOnClick()`: 设置点击回调
- `imgui.setOnCheck()`: 设置复选框回调
- `imgui.setOnSliderEvent()`: 设置滑动条值变化事件回调

## 添加选项
- `imgui.addOptionItem(handle, item_text)`

## 添加单选选项
- `imgui.addRadioBox(handle, text, wrapline)`

## 向标签栏添加标签页
- `imgui.addTabBarItem(tabBar_handle, title)`

## 清空表格所有行
- `imgui.clearTable(handle)`

## 关闭imgui框架
- `imgui.close()`

## 创建位图形状
- `imgui.createBitmapShape(x,y,width,height,bitmap)`

## 创建按钮控件
- `imgui.createButton(parent, label, width, height)`

## 创建复选框控件
- `imgui.createCheckBox(parent, label, checked)`

## 创建圆形
- `imgui.createCircle(x, y, radius, color, filled, segments)`

## 创建颜色选择器控件
- `imgui.createColorPicker(parent, title, color, width, height)`

## 创建组合框控件
- `imgui.createComboBox(parent_handle, items, [width])`

## 创建水平布局容器
- `imgui.createHorticalLayout(parent, width, height)`

## 创建图片显示控件
- `imgui.createImage(parent, path, width, height)`

## 创建文本输入框
- `imgui.createInputText(parent, label, value, inputType, width, height)`

## 创建文本标签控件
- `imgui.createLabel(parent, text, singleline)`

## 创建线段
- `imgui.createLine(x1,y1,x2,y2,color,thickness)`

## 创建多边形
- `imgui.createPolygon(points, color, closed, filled, thickness)`

## 创建水平进度条控件
- `imgui.createProgressBar(parent, progress, width, height)`

## 创建单选按钮组
- `imgui.createRadioGroup(parent, label)`

## 创建矩形
- `imgui.createRectangle(x, y, x1, y1, color, filled, rounding)`

## 创建滑动条控件
- `imgui.createSlider(parent, label, min, max, initialPos, width)`

## 创建滑动开关控件
- `imgui.createSwitch(parent, label, checked, height)`

## 创建标签栏控件
- `imgui.createTabBar(parent_handle, title)`

## 创建标签栏控件
- `imgui.createTabBar(parent_handle, title)`

## 创建表格视图
- `imgui.createTableView(parent, title, columns, showheader, width, height)`

## 创建树形布局容器
- `imgui.createTreeBoxLayout(parent, title, width)`

## 创建垂直布局容器
- `imgui.createVerticalLayout(parent, width, height)`

## 创建一个imgui窗口
- `imgui.createWindow(title,x,y,width,height,showclose)`

## 删除表格行
- `imgui.deleteTableRow(handle, row)`

## 销毁一个imgui窗口
- `imgui.destroyWindow(handle)`

## 获取输入框当前文本内容
- `imgui.getInputText(handle)`

## 获取表格行数/组合框子项数/单选框控件子项数
- `imgui.getItemCount(handle)`

## 获取下拉项文本
- `imgui.getItemText(combo_handle, item_index)`

## 获取进度条当前进度值
- `imgui.getProgressBarPos(handle)`

## 获取组合框/表被选中的项
- `imgui.getSelectedItemIndex(handle)`

## 获取滑动条当前位置
- `imgui.getSlider(handle, pos)`

## 获取表格单元格文本
- `imgui.getTableItemText(handle, row, col)`

## 获取窗口位置和尺寸
- `imgui.getWindowPos(windowHandle)`

## 插入表格行
- `imgui.insertTableRow(handle, after)`

## 获取复选框/开关控件的当前状态
- `imgui.isChecked(handle)`

## 判断图形是否可见
- `imgui.isShapeVisibility(handle)`

## 检测IMGUI支持状态
- `imgui.isSupport()`

## 获取控件当前可见状态
- `imgui.isWidgetVisible(handle)`

## 清空组合框所有项
- `imgui.removeAllItems(combo_handle)`

## 删除组合框指定项
- `imgui.removeItemAt(handle, item_index)`

## 删除图形对象
- `imgui.removeShape(handle)`

## 设置控件同行显示
- `imgui.sameLine(handle, spacing)`

## 设置位图形状
- `imgui.setBitmapShape(handle,bitmap)`

## 设置复选框/开关控件的选中状态
- `imgui.setChecked(handle, state)`

## 设置imgui颜色主题
- `imgui.setColorTheme(style)`

## 设置或更换显示图片
- `imgui.setImage(handle, path)`

## 设置图像对象
- `imgui.setImageFromBitmap(handle, bitmap)`

## 设置输入框文本内容
- `imgui.setInputText(handle, text)`

## 设置输入框类型
- `imgui.setInputType(handle, inputType)`

## 设置组合框|表选中项
- `imgui.setItemSelected(handle, index)`

## 设置容器布局控件是否显示边框
- `imgui.setLayoutBorderVisible(handle, visible)`

## 设置复选框状态变更回调
- `imgui.setOnCheck(handle, callback)`

## 设置按钮点击事件回调
- `imgui.setOnClick(buttonHandle, callback)`

## 设置窗口关闭回调
- `imgui.setOnClose(window_handle, callback_function)`

## 设置组合框/单选框选择事件回调
- `imgui.setOnSelectEvent(handle, callback)`

## 设置滑动条值变化事件回调
- `imgui.setOnSliderEvent(handle, callback)`

## 设置进度条当前进度值
- `imgui.setProgressBarPos(handle, progress)`

## 设置图形位置
- `imgui.setShapePosition(handle,x,y)`

## 设置文本背景
- `imgui.setShapeTextBackground(handle, bg_color, has_bg)`

## 设置文本颜色
- `imgui.setShapeTextColor(handle, new_color)`

## 设置文本图形字体缩放比例
- `imgui.setShapeTextFontScale(handle, scale)`

## 修改文本内容
- `imgui.setShapeTextString(handle, new_text)`

## 设置形状边框厚度
- `imgui.setShapeThickness(handle, thickness)`

## 设置图形可见性
- `imgui.setShapeVisibility(handle, visible)`

## 设置滑动条位置
- `imgui.setSlider(handle, pos)`

## 设置表头文本
- `imgui.setTableHeaderItem(handle, col, text)`

## 设置表格单元格文本
- `imgui.setTableItemText(handle, row, col, text)`

## 设置imgui控件的相关颜色
- `imgui.setWidgetColor(handle, type, color)`

## 设置控件尺寸
- `imgui.setWidgetSize(handle, width, height)`

## 设置imgui控件一些属性
- `imgui.setWidgetStyle(handle, style, v1,v2)`

## 设置控件可见性
- `imgui.setWidgetVisible(handle, visible)`

## 设置窗口标志
- `imgui.setWindowFlags(handle, flags)`

## 设置窗口位置
- `imgui.setWindowPos(handle, x, y)`

## 设置窗口大小
- `imgui.setWindowSize(handle, width, height)`

## 显示UI框架
- `imgui.show([touchable][,font][,fontsize])`

## 创建并显示独立窗口
- `imgui.showWindow(config)`

# JSON方法

## 数据转换
- `jsonLib.decode(json_str)`: JSON字符串转Lua表
- `jsonLib.encode(lua_table)`: Lua表转JSON字符串

## 数据处理
- `jsonLib.isValid(json_str)`: 验证JSON有效性
- `jsonLib.prettify(json_str)`: 格式化JSON字符串

# 字符串方法

## 基础字符串操作
- `string.byte(s)`: 获取字符ASCII码
- `string.char(...)`: ASCII码转字符
- `string.len(s)`: 获取字符串长度
- `string.lower(s)`: 转小写
- `string.upper(s)`: 转大写
- `string.reverse(s)`: 字符串反转
- `string.rep(s,n)`: 字符串重复
- `string.format(fmt,...)`: 格式化字符串

## 字符串查找与分割
- `string.find(s,pattern)`: 查找子串
- `splitStr(src,sep)`: 分割字符串

## UTF8字符串处理
- `utf8.length(s)`: UTF8字符串长度
- `utf8.left(s,n)`: 取左侧n字符
- `utf8.right(s,n)`: 取右侧n字符
- `utf8.mid(s,start,len)`: 取中间子串
- `utf8.inStr(s,sub)`: 查找子串位置
- `utf8.strReverse(s)`: UTF8字符串反转
- `utf8.inStrRev(s, pattern)`: 反向查找UTF8子串
- `utf8.strCut(s, start, len)`: 移除UTF8子串

# FFI库方法

## 基础功能
- `ffi.cdef(str)`: 定义C语言类型和函数
- `ffi.load(name)`: 加载动态库
- `ffi.fill(dst,len,value)`: 内存填充

## 类型定义
- `ffi.typeof(ctype)`: 获取C类型
- `ffi.new(ctype)`: 创建C类型变量
- `ffi.cast(ctype,value)`: 类型转换

## 内存操作
- `ffi.copy(dst,src,len)`: 内存拷贝
- `ffi.fill(dst,len,value)`: 内存填充
- `ffi.string(ptr,len)`: 指针转字符串

# IFS库方法

## 文件系统操作
- `lfs.attributes(path)`: 获取文件/目录属性
- `lfs.chdir(path)`: 切换工作目录
- `lfs.currentdir()`: 获取当前工作目录
- `lfs.link(src, dest)`: 创建硬链接
- `lfs.lock_dir(path)`: 锁定目录
- `lfs.symlinkattributes(path)`: 获取符号链接属性
- `lfs.touch(path)`: 更新文件时间戳
- `lfs.mkdir(path)`: 创建目录
- `lfs.rmdir(path)`: 删除空目录

## 文件锁控制
- `lfs.lock(file, mode)`: 锁定文件
- `lfs.unlock(file)`: 解锁文件

## 目录管理
- `lfs.mkdir(path)`: 创建目录
- `lfs.rmdir(path)`: 删除空目录

## 高级功能
- `lfs.link(src, dest)`: 创建硬链接
- `lfs.symlinkattributes(path)`: 获取符号链接属性
- `lfs.touch(path)`: 更新文件时间

# LuaOpenCV扩展方法

## 内存管理
- `cv.deletePtr(ptr)`: 释放指针内存
- `cv.newByte(val)`: 创建字节指针
- `cv.newDouble(val)`: 创建双精度浮点指针
- `cv.newFloat(val)`: 创建单精度浮点指针
- `cv.newInt(val)`: 创建整型指针
- `cv.newLong(val)`: 创建长整型指针
- `cv.newPoint(x,y)`: 创建Point指针
- `cv.newPoint2f(x,y)`: 创建Point2f指针

## 指针操作
- `cv.getByte(ptr)`: 获取字节值
- `cv.getDouble(ptr)`: 获取双精度值
- `cv.getFloat(ptr)`: 获取单精度值
- `cv.getInt(ptr)`: 获取整数值
- `cv.getLong(ptr)`: 获取长整数值
- `cv.getPoint(ptr)`: 获取Point值
- `cv.getPoint2f(ptr)`: 获取Point2f值
- `cv.setByte(ptr,val)`: 设置字节值
- `cv.setDouble(ptr,val)`: 设置双精度值
- `cv.setFloat(ptr,val)`: 设置单精度值
- `cv.setInt(ptr,val)`: 设置整数值
- `cv.setLong(ptr,val)`: 设置长整数值
- `cv.setPoint(ptr,x,y)`: 设置Point值
- `cv.setPoint2f(ptr,x,y)`: 设置Point2f值

## 核心功能
- `cv.snapShot(l,t,r,b)`: 区域截图返回Mat矩阵